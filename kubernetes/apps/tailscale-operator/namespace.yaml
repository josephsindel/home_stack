apiVersion: v1
kind: Namespace
metadata:
  name: tailscale
  labels:
    # Tailscale proxy pods (operator-created) need privileged securityContext
    # (sysctler init sets net.* sysctls); the cluster default PSA is restricted.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/warn: privileged

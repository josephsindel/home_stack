apiVersion: v1
kind: Namespace
metadata:
  name: pihole
  labels:
    # The secondary Pi-hole uses hostNetwork to answer on the worker's own IP,
    # which the baseline PSS forbids.
    pod-security.kubernetes.io/enforce: privileged

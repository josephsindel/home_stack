apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bazarr
  namespace: media
spec:
  interval: 1h
  install:
    remediation: { retries: 3 }
  upgrade:
    remediation: { retries: 3 }
  chart:
    spec:
      chart: app-template
      version: "5.0.1"
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controllers:
      bazarr:
        containers:
          app:
            image:
              repository: ghcr.io/linuxserver/bazarr
              tag: latest
            env:
              PUID: "1024"
              PGID: "100"
              TZ: America/Los_Angeles
            probes:
              liveness: { enabled: true }
              readiness: { enabled: true }
    service:
      app:
        controller: bazarr
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: "192.168.86.251"
        ports:
          http: { port: 80, targetPort: 6767 }
    persistence:
      config:
        enabled: true
        storageClass: local-path
        accessMode: ReadWriteOnce
        size: 2Gi
        globalMounts: [{ path: /config }]
      media:
        enabled: true
        existingClaim: media
        globalMounts: [{ path: /mnt/nas/content, subPath: content, readOnly: true }]

apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: prowlarr
  namespace: media
spec:
  interval: 1h
  install:
    remediation: { retries: 3 }
  upgrade:
    remediation: { retries: 3 }
  chart:
    spec:
      chart: app-template
      version: "5.0.1"
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controllers:
      prowlarr:
        containers:
          app:
            image:
              repository: ghcr.io/linuxserver/prowlarr
              tag: latest
            env:
              PUID: "1024"
              PGID: "100"
              TZ: America/Los_Angeles
            probes:
              liveness: { enabled: true }
              readiness: { enabled: true }
    service:
      app:
        controller: prowlarr
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: "192.168.86.250"
        ports:
          http: { port: 80, targetPort: 9696 }
    persistence:
      config:
        enabled: true
        storageClass: local-path
        accessMode: ReadWriteOnce
        size: 2Gi
        globalMounts: [{ path: /config }]
